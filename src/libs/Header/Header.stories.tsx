import { Meta, StoryObj } from "@storybook/react";
import { Header } from "./Header";

const header: Meta<typeof Header> = { component: Header };
export default header;

export const ExampleHeader: StoryObj<typeof Header> = {};
